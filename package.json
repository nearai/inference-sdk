{
  "name": "verification-sdk",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsup",
    "check": "tsc",
    "lint": "eslint 'src/**/*.ts' '*.mjs' --fix",
    "prettier": "prettier 'src/**/*.ts' '*.mjs' --write --list-different",
    "prepare": "husky",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "ethers": "^6.15.0",
    "jose": "^6.1.0",
    "sha256": "^0.2.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.23.0",
    "@types/sha256": "^0.2.2",
    "eslint": "^9.23.0",
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3",
    "tsup": "^8.5.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.35.1"
  }
}
