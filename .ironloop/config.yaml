# Review, Resolve, and Test start Agent Runs only for pull requests whose head belongs to this repository.
version: 1

implementer: {}

reviewer:
  auto_review:
    enabled: true

resolver:
  auto_resolve:
    enabled: true
