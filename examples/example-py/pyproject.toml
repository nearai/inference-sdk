[project]
name = "example-py"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.13.0",
    "nearai-inference-sdk",
]

[tool.uv.sources]
nearai-inference-sdk = { path = "../../py", editable = true }
