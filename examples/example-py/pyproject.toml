[project]
name = "example-py"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1,<0.29",
    "openai>=2.0.0,<3",
    "nearai-inference-sdk",
]

[tool.uv.sources]
nearai-inference-sdk = { path = "../../py", editable = true }
